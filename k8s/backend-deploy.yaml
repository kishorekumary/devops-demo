apiVersion: apps/v1
kind: Deployment     
metadata:
  name: backend
  labels:            
    app: backend
spec:
  replicas: 1         
  selector:
    matchLabels:
      app: backend
  template:     
    metadata:
      labels:          
        app: backend
    spec:     
      containers:
      - name: backend
        image:  ykishore/backend:${RELEASE_TAG}
        resources:
          requests:
            memory: "256Mi"
            cpu: "500m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
        imagePullPolicy: Always
        ports:
          - name: http
            containerPort: 9001
        env:
          - name: MYSQL_DATABASE
            valueFrom:        
              configMapKeyRef:
                name: config-envs
                key: MYSQL_DATABASE
          - name: CORS_ALLOW_URL
            valueFrom:        
              configMapKeyRef:
                name: config-envs
                key: CORS_ALLOW_URL
          - name: MYSQL_ROOT_USER
            valueFrom:        
              configMapKeyRef:
                name: config-envs
                key: MYSQL_ROOT_USER
          - name: MYSQL_CONNECTION
            valueFrom:        
              configMapKeyRef:
                name: config-envs
                key: MYSQL_CONNECTION
          
          - name: MYSQL_ROOT_PASSWORD
            valueFrom:
             secretKeyRef:
               name: mysql-secret
               key: password
      restartPolicy: Always
      imagePullSecrets:
        - name: nexus-creds
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
spec:              
  type: NodePort
  selector:
    app: backend
  ports:
  - name: http
    port: 9001
    targetPort: 9001
    nodePort: 32345